# Architecture

The current deep architecture reference lives in [`docs/architecture.md`](docs/architecture.md).

This root file is kept as a compatibility pointer for older assistant prompts and links. Do not add new architecture detail here; update `docs/architecture.md`, `docs/conventions.md`, `CLAUDE.md`, and `AGENTS.md` together when architecture or conventions change.
